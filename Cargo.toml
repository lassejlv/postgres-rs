[package]
name = "postgres-rs"
version = "0.1.0"
edition = "2024"
description = "A fast, lightweight, PostgreSQL wire-compatible database written in Rust"

[dependencies]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
